import 'package:go_router/go_router.dart';

class AppRouter {
  static final router = GoRouter(
    routes: [
      // GoRoute(path: '/', builder: (context, state) => const ArticlesListPage()),
      // GoRoute(path: '/login', builder: (context, state) => const LoginPage()),
    ],
  );
}
